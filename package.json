{
  "name": "multiplier-proxy",
  "version": "1.0.0",
  "description": "Cloudflare Worker for proxying Multiplier under /multiplier subdirectory",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.0",
    "vitest": "^2.1.0",
    "wrangler": "^3.78.0"
  }
}

